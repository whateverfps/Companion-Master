#!/usr/bin/env node
import{execFile}from'node:child_process';import{mkdir,readFile,writeFile}from'node:fs/promises';import{join,resolve}from'node:path';import{fileURLToPath,pathToFileURL}from'node:url';import{promisify}from'node:util';
import{BUILDING_61_DRAWING_CATALOG}from'../src/building-61-drawing-catalog.js';import{buildDrawingCatalog,compareCatalogs}from'../src/drawing-index-engine.js';
const execFileAsync=promisify(execFile),ROOT=resolve(fileURLToPath(new URL('..',import.meta.url))),DATA=join(ROOT,'project-data/bedford'),SOURCE=join(ROOT,'project-documents/bedford/drawings'),MANIFEST=join(DATA,'manifests/campus-drawing-manifest.json'),OUT=join(DATA,'drawing-catalogs');
const JXA=`function run(argv){ObjC.import('PDFKit');var d=$.PDFDocument.alloc.initWithURL($.NSURL.fileURLWithPath($(argv[0]))),pages=[];if(!d)return JSON.stringify({pages:[]});for(var i=0;i<Math.min(10,d.pageCount);i++){var p=d.pageAtIndex(i),s=p?ObjC.unwrap(p.string)||'':'';pages.push({pageNumber:i+1,text:s})}return JSON.stringify({pageCount:Number(d.pageCount),pages:pages})}`;
const json=value=>`${JSON.stringify(value,null,2)}\n`,safe=value=>String(value||'').replace(/[^A-Za-z0-9-]/g,'');
export async function extractIndexCandidatePages(filePath,{runner=execFileAsync}={}){const{stdout}=await runner('osascript',['-l','JavaScript','-e',JXA,filePath],{maxBuffer:8*1024*1024});return JSON.parse(stdout.trim());}
export async function buildBedfordDrawingCatalogs({force=false,building='',manifestPath=MANIFEST,dataDir=DATA,sourceDir=SOURCE,extractor=extractIndexCandidatePages,generatedAt=new Date().toISOString()}={}){const manifest=JSON.parse(await readFile(manifestPath,'utf8')),outputDir=join(dataDir,'drawing-catalogs');await mkdir(outputDir,{recursive:true});const results=[];for(const drawingSet of manifest.drawingSets.filter(item=>item.documentType==='drawing-set'&&(!building||item.buildingId===building))){const outputPath=join(outputDir,`building-${safe(drawingSet.buildingId)}.json`);if(!force){try{const cached=JSON.parse(await readFile(outputPath,'utf8'));if(cached.sourceFingerprint===drawingSet.fingerprint&&(drawingSet.buildingId!=='61'||cached.oracleComparison)){results.push({buildingId:drawingSet.buildingId,status:'cached',catalog:cached,outputPath});continue;}}catch{}}const started=performance.now();try{const extracted=await extractor(join(sourceDir,drawingSet.sourceFileName)),sourcePageCount=Math.max(0,Number(extracted.pageCount)||Number(drawingSet.pageCount)||0),catalog=buildDrawingCatalog({drawingSet:{...drawingSet,pageCount:sourcePageCount},pages:extracted.pages,generatedAt});if(Number(drawingSet.pageCount)!==sourcePageCount)catalog.reviewItems.push(`Intake manifest page count ${drawingSet.pageCount} was reconciled to source PDF count ${sourcePageCount}.`);catalog.durationMs=Math.max(0,performance.now()-started);if(drawingSet.buildingId==='61'){catalog.oracleComparison=compareCatalogs(catalog.sheets,BUILDING_61_DRAWING_CATALOG);results.push({buildingId:'61',status:catalog.oracleComparison.matches?'oracle-match':'needs-review',catalog,outputPath});continue;}await writeFile(outputPath,json(catalog));results.push({buildingId:drawingSet.buildingId,status:catalog.status,catalog,outputPath});}catch(error){results.push({buildingId:drawingSet.buildingId,status:'failed',error:error.message,catalog:null,outputPath});}}const summary={schemaVersion:1,generatedAt,total:results.length,ready:results.filter(item=>['ready','cached','oracle-match'].includes(item.status)).length,needsReview:results.filter(item=>item.status==='needs-review').length,missingIndex:results.filter(item=>item.catalog?.status==='missing-index').length,failed:results.filter(item=>item.status==='failed').length,results:results.map(item=>({buildingId:item.buildingId,status:item.status,sheetCount:item.catalog?.sheets?.length||0,confidence:item.catalog?.confidence||0,validationErrors:item.catalog?.validation?.errors||[],reviewCount:item.catalog?.reviewItems?.length||0,durationMs:item.catalog?.durationMs||0}))};await mkdir(join(dataDir,'reports'),{recursive:true});await writeFile(join(dataDir,'reports/drawing-catalog-report.json'),json(summary));return{results,summary};}
export async function run(argv=process.argv.slice(2)){const force=argv.includes('--force'),building=(argv.find(arg=>arg.startsWith('--building='))||'').slice(11),result=await buildBedfordDrawingCatalogs({force,building});for(const item of result.summary.results)console.log(`${item.buildingId.padEnd(16)} ${item.status.padEnd(14)} ${String(item.sheetCount).padStart(4)} sheets  ${item.durationMs.toFixed(1)} ms${item.validationErrors.length?`  ${item.validationErrors.join('; ')}`:''}`);console.log(`Catalogs ${result.summary.total} | ready ${result.summary.ready} | review ${result.summary.needsReview} | missing index ${result.summary.missingIndex} | failed ${result.summary.failed}`);return result.summary.failed?1:0;}
if(import.meta.url===pathToFileURL(process.argv[1]||'').href)process.exitCode=await run();
