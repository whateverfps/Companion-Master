{
  "name": "mission-companion-master-web",
  "version": "2.1.0",
  "private": true,
  "description": "Source-grounded subject matter expert workspace",
  "scripts": {
    "test": "node --check src/app.js && node --check src/engine.js && node --check src/parsers.js && node --check src/retrieval.js && node --check src/diagnostics.js",
    "serve": "python3 -m http.server 8000"
  }
}
